server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: awss3
  cloud:
    config:
      server:
        s3:
          bucket: ums-config-file-bucket
          label: ums-config-file
