server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: awss3
  cloud:
    config:
      server:
        awss3:
          region: ap-northeast-2
          bucket: ums-config-file-bucket
          prefix: ums-config-file/