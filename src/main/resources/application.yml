server:
  port: 8889
spring:
  application:
    name: config-server
  profiles:
    active: awss3
  cloud:
    config:
      server:
        awss3:
          region: ap-northeast-2
          bucket: ums-proj-bucket-01
          prefix: ums-config-file/