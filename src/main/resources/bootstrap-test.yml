spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ums-messaging/config-file-server.git
          default-label: main
          clone-on-start: true
  config:
    activate:
      on-profile: test
